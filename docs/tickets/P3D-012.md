# P3D-012 — Chunks ExportInfo

- **Status:** TODO
- **Priority:** P3
- **Module:** P3D
- **Depends on:** —
- **Files:** `dev/codegen/p3d.json:934-948` (les 3 **déjà décrits**), `src/P3D/P3DChunk.h:34-36` (ChunkType), `src/P3D/P3D.generated.{h,cpp}` (à régénérer)

## Contexte

Métadonnées d'export (outil DCC, paramètres), **déjà entièrement décrites dans
`p3d.json`** mais **non régénérées** (absentes de `P3D.generated.h`) :

- `ExportInfo` (0x7030) : `version u32`, `name string`, `children: children ExportInfoNamedString` ;
- `ExportInfoNamedString` (0x7031) : `name string`, `value string` ;
- `ExportInfoNamedInteger` (0x7032) : `name string`, `value u32`.

Purement informatif (debug / outillage), d'où la priorité P3. Le seul travail est la
régénération + une éventuelle correction de layout.

## Approche

1. Vérifier que `ExportInfo` agrège bien **les deux** types nommés : actuellement il ne
   référence que `children ExportInfoNamedString`. Ajouter
   `integers: children ExportInfoNamedInteger` si les dumps montrent des entiers nommés.
2. Lancer `dev/codegen/run.bat`, vérifier les 3 classes et `Load()`.
3. Aucune intégration gameplay requise ; éventuellement logguer le contenu pour le debug.

## Critères d'acceptation

- [ ] `ExportInfo`, `ExportInfoNamedString`, `ExportInfoNamedInteger` générés.
- [ ] `ExportInfo` expose à la fois les chaînes et les entiers nommés (si présents).
- [ ] Un `.p3d` contenant un `ExportInfo` se parse sans assert.
