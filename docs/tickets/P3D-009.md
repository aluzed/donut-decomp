# P3D-009 — CollisionWallVolume, CollisionEffect, Atc

- **Status:** TODO
- **Priority:** P1
- **Module:** P3D
- **Depends on:** —
- **Files:** `dev/codegen/p3d.json:929-932` (`CollisionWallVolume` **déjà décrit**), `src/P3D/P3DChunk.h:155,156,181` (ChunkType), `src/P3D/P3D.generated.{h,cpp}`, `src/Physics/`

## Contexte

État inégal des trois chunks de collision/effets :

- `CollisionWallVolume` (0x7010005) : **déjà décrit dans `p3d.json`** (`normal: vec3`,
  `position: vec3`) mais **pas régénéré** (absent de `P3D.generated.h`). Compléter la
  famille `CollisionVolume` (`CollisionBBoxVolume`, `CollisionOBBoxVolume`,
  `CollisionSphere`, `CollisionCylinder` déjà générés).
- `CollisionEffect` (0x3000600) et `Atc` (0x3000602) : déclarés dans l'enum `ChunkType`
  mais **absents de `p3d.json`** → à reverser (effets sonores/visuels au contact, et
  données « Atc » liées au trafic/animation des collisions).

## Approche

1. **CollisionWallVolume** : lancer `dev/codegen/run.bat`, puis l'intégrer dans le parsing
   de `CollisionVolume` (généré) — actuellement `CollisionVolume` route vers `bBox`,
   `obBox`, `sphere`, `cylinder` ; ajouter le sous-volume `wall` dans `p3d.json` au besoin
   et le consommer côté `src/Physics/`.
2. **CollisionEffect** / **Atc** : reverser les layouts (Ghidra / dumps `.p3d`). Décrire
   dans `p3d.json` avec la grammaire en place, régénérer, vérifier les `Load()`.
3. Brancher la lecture là où les volumes de collision sont consommés (résolution physique).

## Critères d'acceptation

- [ ] `CollisionWallVolume` généré et rattaché à la hiérarchie `CollisionVolume`.
- [ ] `CollisionEffect` et `Atc` décrits dans `p3d.json`, reversés, et générés.
- [ ] Un `.p3d` de collision contenant un mur se parse sans assert.
- [ ] Le volume mural (`normal` + `position`) est exploitable côté physique.
